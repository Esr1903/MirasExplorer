import Link from "next/link";
import { notFound } from "next/navigation";
const API=process.env.NEXT_PUBLIC_API_BASE_URL??"http://127.0.0.1:8000";
type Detail={id:string;title:string;description?:string|null;metadata:{label:string;value:string}[];media:{id:string;url:string;alt:string}[];related:{id:string;title:string;kind:string}[]};
export default async function HeritageDetailPage({params}:{params:Promise<{id:string}>}) { const {id}=await params; const response=await fetch(`${API}/api/public/assets/${id}`,{cache:"no-store"}); if(response.status===404)notFound(); if(!response.ok)return <main className="heritage-detail-page"><div className="miras-container"><p>Katalog servisine şu anda erişilemiyor.</p></div></main>; const record:Detail=await response.json(); return <main className="heritage-detail-page"><div className="miras-container"><nav className="heritage-detail-breadcrumb"><Link href="/">Ana Sayfa</Link><span>/</span><Link href="/ara">Arama</Link><span>/</span><span>{record.title}</span></nav></div><section className="heritage-detail-hero"><div className="miras-container heritage-detail-hero__grid"><div className="heritage-detail-hero__content"><p className="miras-eyebrow">Taşınabilir kültürel miras</p><h1 className="heritage-detail-hero__title">{record.title}</h1>{record.description?<p className="heritage-detail-hero__summary">{record.description}</p>:null}</div><div className="heritage-detail-hero__media">{record.media[0]?<img src={`${API}${record.media[0].url}`} alt={record.media[0].alt}/>:<div className="heritage-detail-primary-media--empty">Görsel bulunmuyor</div>}</div></div></section><section className="heritage-detail-content"><div className="miras-container heritage-detail-content__grid"><main className="heritage-detail-main">{record.media.length>1?<section className="heritage-detail-section"><h2>Görseller</h2><div className="heritage-detail-gallery">{record.media.map(m=><img key={m.id} src={`${API}${m.url}`} alt={m.alt}/>)}</div></section>:null}{record.related.length?<section className="heritage-detail-section"><h2>İlişkili kayıtlar</h2><div className="heritage-detail-relations">{record.related.map(r=><Link key={r.id} href={r.kind==="heritage_asset"?`/eser/${r.id}`:"/ara"}>{r.title}</Link>)}</div></section>:null}</main><aside className="heritage-detail-sidebar"><section className="heritage-detail-info-card"><h2>Eser künyesi</h2><dl className="heritage-detail-metadata">{record.metadata.filter(m=>m.value).map(m=><div className="heritage-detail-metadata__row" key={m.label}><dt>{m.label}</dt><dd>{m.value}</dd></div>)}</dl></section></aside></div></section></main>; }
